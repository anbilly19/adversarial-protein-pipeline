# Protein-specific
biopython>=1.81          # PDB parsing

# Transformer / ESM-2 (CPU-only, no CUDA required)
transformers>=4.36.0
torch>=2.0.0             # CPU builds are fine; no GPU needed

# Scientific computing
numpy>=1.24.0
scipy>=1.11.0

# Utilities
tqdm>=4.65.0
requests>=2.31.0
